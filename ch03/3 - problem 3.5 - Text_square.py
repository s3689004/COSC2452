def textsquare(character, number):
  space = " "
  print(character * number)
  print(character + space * (number -2) + character)
  print(character + space * number + character)
  print(character + space * number + character)
  print(character + space * number + character)
  print(character + space * number + character)
  print(character * number)
#  for i in number: